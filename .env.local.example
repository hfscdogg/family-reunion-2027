# Vercel Postgres connection string
# When using Vercel Postgres integration, these are set automatically.
# For local dev, copy this file to .env.local and fill in your values.
POSTGRES_URL="postgres://user:password@localhost:5432/family_reunion"

# Secret token to protect the /api/init endpoint
INIT_SECRET="pick-a-strong-random-string"
